# TODO
- [ ] Update ContactForm to POST JSON to our Next.js API route (`/api/contact`) instead of FormSubmit.co.
- [ ] Preserve existing zod + react-hook-form validation and honeypot field.
- [ ] Handle API response `{ success: boolean, message: string }` and show success UI on success, alert on failure.
- [ ] Ensure redirect/thank-you behavior matches current UI flow (stay on form with success screen unless app expects otherwise).
- [ ] Run `npm run lint` and `npm run build` (and fix any TS/ESLint issues).

